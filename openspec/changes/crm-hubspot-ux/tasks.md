# Tasks: CRM HubSpot UX and Unified Contact

## Review Workload Forecast

| Field | Value |
|---|---|
| Estimated changed lines | ~5,920 across 18 PRs (Phase 4B added) |
| 400-line budget risk | High |
| Chained PRs recommended | Yes |
| Suggested split | PR 1 → PR 14 (see table below) |
| Delivery strategy | ask-on-risk (resolved: chained) |
| Chain strategy | feature-branch-chain |

Decision needed before apply: No (owner already resolved chain strategy)
Chained PRs recommended: Yes
Chain strategy: feature-branch-chain
400-line budget risk: High

**Tracker branch**: `feat/crm-hubspot-ux` (draft, no-merge; only this merges to `main`). PR 1 targets the tracker; each later PR targets the immediately previous PR's branch. Owner reviews via Vercel preview deploys.

### Suggested Work Units

| PR | Title | Slice | Base branch | Est. lines | Gate |
|---|---|---|---|---|---|
| 0 | Commit dark-mode removal | precondition | `main` → tracker | ~small | none (precondition for PR 8) |
| 1 | Schema 0013, `bd.role`, `requireAdmin`, audit tables | 0 | tracker | ~220 | none |
| 2 | Identity matcher module + tests | 0 | PR 1 | ~320 | TDD |
| 3 | Collapse planner, dry-run script, `/admin/migration` | 0 | PR 2 | ~380 | **Dry-run report approval** |
| 4 | Fold leads, re-point refs, status backfill | 0b | PR 3 | ~360 | **Dry-run report approval** |
| 4B-1 | Identity resolver core (`planIdentityWrites`/`applyIdentityWrites`) | 0b | PR 4 | ~300 | TDD |
| 4B-2 | Contact/lead live write cutover under `pg_advisory_xact_lock` | 0b | PR 4B-1 | ~260 | TDD |
| 4B-3 | Reference writes (activity/task/signal), status/owner rules | 0b | PR 4B-2 | ~240 | TDD |
| 4B-4 | Catch-up phase (`--phase=catch_up`) | 0b | PR 4B-3 | ~320 | **Dry-run approval** |
| 5 | `deriveStatus`, activity writes, outreach/hiring re-scope | 0b | PR 4B-4 | ~350 | TDD |
| 6 | Merge/unmerge engine + tests | 1 | PR 5 | ~300 | TDD |
| 7 | `/admin/duplicates` UI | 1 | PR 6 | ~280 | **Mockup approval** |
| 8 | App shell, tokens, remove LocaleSwitcher/`en` path | UI | PR 7 | ~300 | **Mockup approval**; requires PR 0 |
| 9 | Record: shell + About pane | 2 | PR 8 | ~350 | **Mockup approval** |
| 10 | Record: timeline filter + log dialogs | 2 | PR 9 | ~350 | **Mockup approval** |
| 11 | Record: associations, admin view, redirects | 2 | PR 10 | ~350 | **Mockup approval** |
| 12 | List: views and filters | 3 | PR 11 | ~350 | **Mockup approval** |
| 13 | List: columns and bulk actions | 3 | PR 12 | ~350 | **Mockup approval** |
| 14 | List: board and import | 3 | PR 13 | ~350 | **Mockup approval** |

## Phase 0: Precondition

- [ ] 0.1 Commit or discard the uncommitted dark-mode removal (`src/lib/theme/*` deletions, `UserMenu.tsx`, page/dictionary edits currently in the working tree) on `main`, before creating the tracker branch. Blocks PR 8.
- [ ] 0.2 Create draft tracker branch `feat/crm-hubspot-ux` off `main` (no-merge until all child PRs land).

## Phase 1: Foundation — Schema & Roles (PR 1, base: tracker)

- [x] 1.1 Write migration `drizzle/0013_unified_person.sql`: `person`, `person_bd_connection`, `person_property_history`, `person_id_map`, `merge_event`, `duplicate_candidate`, `audit_log`, `migration_run`, `saved_view`, plus `person_id`/`actor_bd_id` FK columns on `activity`, `task`, `signal`, `linkedin_scrape_job`.
- [x] 1.2 Add `bd.role text not null default 'bd'` column and seed the owner as `admin`.
- [x] 1.3 Add `requireAdmin()` guard in `src/lib/auth/` reusing `getCurrentBd()`.
- [x] 1.4 Test: `requireAdmin()` rejects non-admin, allows admin (`tests/unit/`).

## Phase 2: Identity Matcher (PR 2, base: PR 1)

- [x] 2.1 RED: write failing tests for `src/lib/identity/matcher.ts` per contact-identity spec scenarios (profile-key auto-merge, verified-email auto-merge, name+company → review, own-company skip, no-email/no-LinkedIn fallback).
- [x] 2.2 GREEN: implement `matcher.ts` with `IdentityIndex` interface and `MatchResult` union (design D3).
- [x] 2.3 RED/GREEN: `mergeProperties(a, b)` pure function — richer/verified/longer wins, ties go to most recent, losers recorded (contact-identity R7).
- [x] 2.4 REFACTOR: extract shared normalization helpers reused from `src/lib/csv.ts` (`normalizeProfileKey`) and `src/lib/ownCompany.ts`.

## Phase 3: Collapse Migration + Dry-Run Gate (PR 3, base: PR 2)

- [x] 3.1 RED/GREEN: migration planner (rows → plan + counts) as a pure function, tested against fixtures (3-BDs-1-profile-key scenario).
- [x] 3.2 `scripts/unify-contacts.ts --phase=collapse --dry-run`: builds in-memory `IdentityIndex`, writes `migration_run` report (auto-merged/flagged/new counts per table), no writes to `person`/`person_bd_connection` outside dry-run mode.
- [x] 3.3 `/admin/migration` page (admin-only, `requireAdmin`): shows latest dry-run report, "Approve dry run" action writing `audit_log(migration_approve)` and `migration_run.approved_by/at`.
- [x] 3.4 `--execute --run=<id>`: refuses if `input_hash` changed or run not approved; snapshot production backup first; tags rows with `migration_run_id`.
- [x] 3.5 **GATE**: owner reviews the collapse dry-run report in `/admin/migration` (against production data via Vercel preview) and records approval before `--execute` runs. (Executed in prod: migration_run b9003aae, kind=collapse, mode=execute.)
- [x] 3.6 Test: dry-run mode never writes `person` rows; execute mode refuses on stale `input_hash` or missing approval.

## Phase 4: Fold Leads (PR 4, base: PR 3)

- [x] 4.1 RED/GREEN: fold planner — leads matched via matcher, unmatched leads become new Contacts carrying `ownerBdId` and source. (`src/lib/migration/foldPlanner.ts`; IdentityIndex seeded from EXISTING `person` rows, since Phase 3 collapse is already executed in prod.)
- [x] 4.2 RED/GREEN: `status_backfill` activity writer for leads with a manually set status and no supporting activity (contact-migration spec scenario). (`planStatusBackfills` in `src/lib/migration/foldPlanner.ts`; no DB migration needed — `activity.type` is free text, not an enum.)
- [x] 4.3 `scripts/unify-contacts.ts --phase=fold_leads`: re-points `activity`/`task`/`signal` via `person_id_map` (`linkedin_scrape_job` has no lead-scoped column, so it is not re-pointed here); produces fold dry-run report.
- [ ] 4.4 **GATE**: owner reviews the fold-leads dry-run report before `--execute`.
- [x] 4.5 Test: zero orphaned references after fold execute (fixture-level check); every legacy id resolves via `person_id_map`.

**Actual PR slicing (recorded as it happened, deviates from the plan table above)**:

- 4a — planner (PR #8: `feat/crm-hubspot-ux-04-fold-leads`).
- 4b — collapse-merge planner fix (`feat/crm-hubspot-ux-04b-collapse-merge-fix`). Repair verified unnecessary: dry run against prod showed 0 diffs across all 699 multi-contact groups (identical values in the 8 affected fields), so no `--execute` was needed and no repair CLI shipped.
- 4d — fold-leads write path (`feat/crm-hubspot-ux-04d-fold-write`): planner + write rows + run orchestration, wired to the database.
- 4e — fold-leads CLI wiring (`feat/crm-hubspot-ux-04e-fold-cli`): `--phase=fold_leads` reaches the real database via `scripts/unify-contacts.ts`; re-pointing scope docs fix (no `linkedin_scrape_job` column).
- 4f — status backfill + admin UI (`feat/crm-hubspot-ux-04f-fold-backfill-admin`): task 4.2's `status_backfill` writer, and `/admin/migration` extended to list/approve `fold_leads` runs alongside `collapse`.

## Phase 4B: Write Cutover & Catch-up (PRs 4B-1..4B-4, base: PR 4)

- [x] 4B.1 RED/GREEN: `planIdentityWrites(rows, index)` in `src/lib/identity/resolve.ts` — new/auto/review/own-company outcomes, intra-chunk dedup, merged-survivor resolution (contact-identity "Live ingestion resolves identity").
- [x] 4B.2 GREEN: `prefetchIdentityIndex(tx, rows)` (3 indexed queries) + `applyIdentityWrites(tx, plan)` in `src/lib/identity/resolveDb.ts` with `ON CONFLICT (profile_key) DO NOTHING RETURNING` and re-select of losers. (PR 4B-1, ~300 code)
- [x] 4B.3 RED/GREEN: `upsertContacts` and `importLeads` run each chunk in one transaction under `pg_advisory_xact_lock`, writing legacy + person + connection + map; `IDENTITY_DUAL_WRITE` kill switch. Implemented as `src/lib/identity/ingestWrite.ts` (pure legacy-row → `IdentityIngestRow` mappers + `runIdentityCutoverChunk` injected-ports orchestration, ordering unit-tested with fakes) wired into `upsertContacts`/`importLeads`: each chunk runs in `db.transaction`, legacy upsert FIRST, then `withIdentityLock` wraps ONLY prefetch/plan/apply (matches design D14's literal ordering; lock is skipped entirely when a chunk has no identity rows). `.returning()` is only appended to the legacy upsert on the dual-write-on branch, so the kill-switch-off path stays the exact original statement (D11: byte-identical legacy write, including query text). Leads with no resolved `ownerBdId` are intentionally left out of the identity rows for a chunk (nothing to connect); the catch-up planner (4B.7, not yet built) is the intended way to re-point them once an owner is assigned.
- [x] 4B.4 Test: two concurrent uploads of the same new profile key yield one `person` and two connections. (PR 4B-2, ~260 code) Implemented as a fake-level test (`tests/unit/identityIngestWrite.test.ts`, "4B.4: two concurrent uploads..."): two sequential calls to `runIdentityCutoverChunk` share an in-memory fake person store that enforces `profile_key` uniqueness + `ON CONFLICT DO NOTHING` the same way `applyIdentityWrites` does, reusing the real `buildIdentityWriteRows`/`repointIdentityWriteRows`. **A real-DB concurrency test (two genuinely parallel transactions) is not possible in this unit-test environment** (no DATABASE_URL, `tests/unit` is `node:test` + fakes only) — this is a fake-level proof that the plan/repoint logic converges to one person + two connections when replayed against a store with the same conflict semantics as Postgres. Leave the real-DB race as an owner/staging verification note before `--execute`/production dual-write is trusted at scale.
- [x] 4B.5 RED/GREEN: `createActivity` (incl. Gmail log), `createTask`/`updateTask`, manual signal set `person_id` + `actor_bd_id` via `person_id_map` subquery. Implemented as a pure `resolvePersonIdLookup` (`src/lib/identity/referenceWrite.ts`, unit-tested) picking the row's legacy subject (contact takes precedence over lead; company-only rows have nothing to map), plus a thin `personIdLookupSql` subquery builder (`src/lib/identity/resolveDb.ts`). Wired into `createActivity`/`createTask`/`updateTask` (`src/lib/activity/queries.ts`, `src/lib/tasks/queries.ts`) and the manual signal route (`src/app/api/signals/manual/route.ts`), all gated by `isIdentityDualWriteEnabled()`. `actor_bd_id` set at the call sites that know the acting BD (`src/app/activity/actions.ts#createActivityAction`, `src/app/tasks/actions.ts#createTaskAction`, the manual signal route). `updateTask` only re-resolves `person_id` when the update itself changes `leadId`/`contactId` (no current caller does, but the query layer supports it per design). No matcher, no advisory lock — these paths never create a person; an unmapped legacy row (created before catch-up) leaves `person_id` null, same as importLeads' owner-less leads (4B.3).
- [x] 4B.6 RED/GREEN: `updateLeadStatus` appends `status_change` activity; `updateLeadOwner` applies R3; `recomputeMessageSignals` updates `person_bd_connection`. (PR 4B-3, ~240 code) Implemented in `src/lib/leads/queries.ts`: `updateLeadStatus` uses a pure `planStatusChangeActivity` (`src/lib/leads/statusChange.ts`, unit-tested: a real status edit produces `{status}` metadata, a notes-only call produces nothing) and, when dual-write is on, appends a `status_change` activity carrying `person_id` (via the same `personIdLookupSql` subquery) and `actor_bd_id` in the same transaction as the legacy `lead` update. `updateLeadOwner` wraps the legacy update and a raw-SQL R3 gate in one transaction: `person.owner_bd_id` is only overridden when the mapped person has zero `person_bd_connection` rows (`NOT EXISTS`), and a `person_property_history` row is written alongside it; when the person already has a connection, R3 (earliest LinkedIn connector) stands and the edit is silently a no-op on the person side (legacy `lead.ownerBdId` still updates). `recomputeMessageSignals` (`src/lib/queries.ts`) gets a third set-based `UPDATE person_bd_connection … FROM peer_agg JOIN contact JOIN person_id_map`, gated by the kill switch, mirroring the existing `contact` aggregate update. The R3 gate and the `person_bd_connection` update are DB-only raw SQL (same "can't unit-test without DATABASE_URL" convention as the rest of this write-cutover's thin DB layer, per 4B.3/4B.4); only the pure planning pieces (`resolvePersonIdLookup`, `planStatusChangeActivity`) are unit-tested.
- [x] 4B.7 RED/GREEN: catch-up planner — anti-join input, lead/contact drift; `input_hash` over input rows. Implemented as `src/lib/migration/catchUpPlanner.ts#planCatchUp(input, existingPersons)` (pure, unit-tested — `tests/unit/catchUpPlanner.test.ts`), reusing `planIdentityWrites` (design D12) and the exact same legacy-row → `IdentityIngestRow` mappers the live cutover uses (`ingestWrite.ts`'s `contactRowsToIdentityRows`/`leadRowsToIdentityRows`), so catch-up can't drift from live-path field mapping. The caller (4B.8) is responsible for querying the anti-join AND drifted rows (leads by `updated_at`, contacts via the existing diff core) and passing both in the same row shape — a drifted row that still matches its existing person's identity keys is planned as an ordinary "auto" update, no special-casing needed. Leads with no resolved owner are excluded from the plan (same decision `leadRowsToIdentityRows` already makes for live ingestion — `bdId` is required for `person_bd_connection`); `leadsSkippedNoOwner` reports how many, so they're visibly still-unmapped rather than silently dropped. `input_hash` (`computeCatchUpInputHash` in `inputHash.ts`) is reflective over every field of every contact/lead row AND the existing-person snapshot the plan was matched against (same rationale as `computeFoldInputHash`). The null-`person_id` reference re-point (`UPDATE … FROM person_id_map`) is a straight set-based SQL join with nothing to plan — deferred to 4B.8's DB layer.
- [x] 4B.8 `--phase=catch_up` dry-run/execute reusing `executionGuard`, `snapshotBackup`, `finalizeExecute` pattern; shown in `/admin/migration`. (PR 4B-4 slice B, branch `feat/crm-hubspot-ux-04B4b-catch-up-cli`, commit `a00658a`, ~359 non-comment code lines) Implemented: `migration_run.kind='catch_up'` added everywhere kinds are enumerated (`MigrationRunKind` in `executionGuard.ts`, `approveGuard`'s type import, `getLatestMigrationRun`/`listMigrationRuns`/`approveMigrationRunAction`'s `isMigrationRunKind`, `/admin/migration`'s new third section, `es`/`en` dictionaries) — `migration_run.kind` is free text (`text("kind")`, no CHECK/enum constraint in `drizzle/0013_unified_person.sql`), so **no schema migration was needed**. `src/lib/migration/catchUpQueries.ts` (DB-only, not unit-tested per the same import-time-`DATABASE_URL` convention as `queries.ts`): `readUnmappedContacts`/`readUnmappedLeads` (keyset-paged anti-join via `NOT EXISTS (person_id_map ...)`), `readDriftedLeads(since)` (already-mapped leads with `updated_at > since`), `readExistingPersonsForCatchUp` (reuses the live cutover's `prefetchIdentityIndex` — 3 indexed queries scoped to this run's rows, never a full `person` scan), `saveCatchUpDryRunReport`, and `finalizeCatchUpExecute` (one transaction: claims the approved run, applies the plan via `applyIdentityWrites` under `withIdentityLock` — task's instructed reuse — then re-points every `activity`/`task`/`signal` row with a null `person_id` via one set-based `UPDATE ... FROM person_id_map` per table matching either legacy subject, marks executed, writes `audit_log(migration_execute)`). `src/lib/migration/catchUpRun.ts` (pure orchestration, unit-tested — `tests/unit/catchUpRun.test.ts`, 5 tests): `runCatchUpDryRun`/`runCatchUpExecute`, mirroring `foldRun.ts`'s port shape and `assertExecutionAllowed(..., "catch_up")` gate (refuses stale hash, wrong kind, missing approval). `scripts/unify-contacts.ts --phase=catch_up` (dry-run default; `--execute --run=<id>`); its `driftSince()` looks back to the latest EXECUTED `catch_up` run, or `fold_leads` on the very first catch-up, or the epoch. **Deviation, reported not silently dropped**: contact drift (design's "contacts, which lack `updated_at`, via the existing diff core") is NOT implemented — no such diff-core function exists in this codebase and `contact` has no `updated_at` column; only the anti-join (never-mapped) contacts/leads and lead drift by `updated_at` are wired. 196/196 unit tests green (was 191 before this batch), clean typecheck.
- [ ] 4B.9 **GATE**: owner confirms deploy sequence (early partial release vs. two catch-ups), reviews catch-up dry-run, approves before `--execute`. **Required order** (now enforced by `src/lib/migration/phaseOrderGuard.ts`, wired into `scripts/unify-contacts.ts`'s `--phase=catch_up` dry-run AND execute): `collapse --execute` → `fold_leads --execute` → deploy the live dual-write cutover (or deploy it earlier — safe now that fold-leads is anti-join safe, see 4B.11) → `catch_up`. **Residual risk**: catch-up's row prefetch runs outside the execute transaction's advisory lock, so a concurrent live write creating the same verified-email person in that window can make `--execute` fail (rolls back cleanly; re-run is safe and only reprocesses what's still unmapped) — recommend running catch-up in a low-traffic window.
- [x] 4B.11 Fold-leads anti-join safety (fresh-review fix, no PR slice of its own — landed with the phase-ordering guard): `readUnmappedLeadRowsForFold` (`src/lib/migration/queries.ts`) reads leads via the same `NOT EXISTS (person_id_map ...)` anti-join catch-up's readers use, instead of every `lead` row, so fold-leads can run after catch-up or after the live cutover has mapped some leads without a `person_id_map` primary-key violation; its `person_id_map` insert also gets `.onConflictDoNothing()` as belt-and-braces. CLI flag parsing (`--phase=`, `--execute`, `--dry-run`, `--run=`) extracted to `src/lib/migration/cliArgs.ts` and made strict: unknown flags error listing valid ones, `--execute`+`--dry-run` together errors.
- [ ] 4B.10 Test: after execute, zero unmapped `contact`/`lead` rows and zero non-own-company references with null `person_id`; re-run reports zero. Left `[ ]` deliberately (not ticked): the planner+orchestration level is now fully covered — fixture-level coverage from 4B.7 (`tests/unit/catchUpPlanner.test.ts`: applying a plan's `buildIdentityWriteRows` output maps every non-skipped row, re-running `planCatchUp` on an empty anti-join input yields an empty plan) plus 4B.8's `runCatchUpExecute`/`finalizeCatchUpExecute` wiring (`readUnmappedContacts`/`readUnmappedLeads` are anti-joins by construction, so a second dry-run against the same data set necessarily reads zero rows). What remains — and is explicitly an OWNER verification step, not something this batch can produce — is running `--phase=catch_up --execute` against the real production database and confirming zero unmapped rows / zero null-`person_id` references there; that requires `DATABASE_URL` and is out of scope for `npm run test:unit`.

## Phase 5: Status Derivation & Re-Scope Reads (PR 5, base: PR 4B-4)

- [x] 5.1 RED/GREEN: pure `deriveStatus(events)` per the rank/discard algorithm in design.md, returning `{ status, because }`. `src/lib/status/deriveStatus.ts` (PR 5a, branch `feat/crm-hubspot-ux-05a-derive-status`), plus the `activityRowToStatusEvent`/`connectionRowToStatusEvent` row builders (PR 5b).
- [x] 5.2 Wire `deriveStatus()` into the same transaction as every activity write, merge, and message import; cache result on `person.status`/`status_activity_id`. `src/lib/status/recompute.ts#recomputePersonStatus` wired into `createActivity` (`src/lib/activity/queries.ts`), `updateLeadStatus` (`src/lib/leads/queries.ts`), and `recomputeMessageSignals` (`src/lib/queries.ts`, per-affected-person loop after its set-based `person_bd_connection` update) — PR 5b, branch `feat/crm-hubspot-ux-05b-status-wiring`. Merge (Phase 6, doesn't exist yet) gets a documented `TODO` hook in `recompute.ts`.
- [x] 5.3 Re-scope outreach and hiring reads off `bdId` filtering onto the unified `person` table (no `bdId` scoping on Contact queries per proposal success criteria). `listOutreachCandidates` (`src/lib/outreach/queries.ts`) and `getCompanyHiringSummaries` (`src/lib/hiring/queries.ts`) — PR 5c, branch `feat/crm-hubspot-ux-05c-rescope-reads`. Visible change: BDs now see and can rank teammates' Contacts in these lists (message/reciprocal signals aggregated across every connected BD, design R4 "combined across BDs"). Known gap, not a regression: the "view"/"generate message" actions for a teammate-exclusive Contact still 404 until Phase 9's unified `/contacts/[id]` record page ships (D8) — `id` prefers the calling BD's own legacy `contact.id` and falls back to `person.id` otherwise, documented in the query's doc comment.
- [x] 5.4 Test: discard un-discarded by a later activity; combined multi-BD activity picks the most advanced stage. `tests/unit/deriveStatus.test.ts` (PR 5a).

## Phase 6: Merge/Unmerge Engine (PR 6, base: PR 5)

- [x] 6.1 RED/GREEN: `mergeContacts(survivorId, mergedId, reason, actorBdId)` — writes `merge_event` snapshot + linked `audit_log(merge)` row in one transaction. Split into two work units: pure planner `src/lib/identity/merge.ts#planMerge` (PR 06a, branch `feat/crm-hubspot-ux-06a-merge-engine`) + thin DB layer `src/lib/identity/mergeDb.ts#mergeContacts` (PR 06b, branch `feat/crm-hubspot-ux-06b-merge-db`, base 06a). `SELECT ... FOR UPDATE` on both person rows guards concurrent merges of the same person; refuses if either already has `merged_into_id` set. Recomputes the survivor's status (`recomputePersonStatuses`) in the same transaction, closing the `recompute.ts` TODO from task 5.2.
- [x] 6.2 RED/GREEN: `unmergeContact(mergeEventId, actorBdId)` — replays the snapshot in reverse; writes `audit_log(unmerge)`. `src/lib/identity/merge.ts#planUnmerge` (pure, PR 06a) + `mergeDb.ts#unmergeContact` (PR 06b). No time limit (duplicate-review spec). **Rule for references/id-map rows created after the merge** (design left this unspecified): they stay on the survivor — `planUnmerge` only reverts the exact rows frozen in `snapshot.movedReferences`/`movedIdMapRows` at merge time, chosen as the minimal-safe interpretation.
- [x] 6.3 RED/GREEN: `markNotDuplicate(pairId, actorBdId)` — sets `duplicate_candidate.status='not_duplicate'`, writes `audit_log(not_duplicate)`; pair never resurfaces. `mergeDb.ts#markNotDuplicate` (PR 06b). Verified all three candidate-creating paths (`src/lib/identity/resolveDb.ts`, `src/lib/migration/queries.ts` x2 for collapse/fold) already `.onConflictDoNothing()` on the unique `(person_a_id, person_b_id)` pair, so a `not_duplicate` row blocks re-insertion regardless of status.
- [x] 6.4 Test: false-merge → unmerge restores both original Contacts fully. `tests/unit/identityMerge.test.ts` ("6.4: false merge then unmerge restores both original Contacts exactly") — pure-level round trip: `planMerge` then `planUnmerge(plan.snapshot)` recovers the original survivor/merged rows, connections, references, id-map rows and duplicate-candidate status byte-for-byte.
- [x] 6.5 Fresh-review fix (PR 06c, branch `feat/crm-hubspot-ux-06c-safe-unmerge`, base 06b): safe field/connection-level unmerge. `planUnmerge` no longer overwrites the survivor with the frozen pre-merge snapshot (was silently reverting later merges/edits) — the snapshot now records, per survivor field the merge changed, both the value before and the value written; unmerge reverts a field only if its current value still equals what the merge wrote, otherwise keeps it and reports "kept (changed after merge)". Same-BD connection conflicts are aggregated onto one row (counts summed, earliest/latest timestamps, flags OR'd) instead of dropping the merged side's facts, with the same revert-if-unchanged rule on unmerge. Connections moved with no conflict are repointed back onto `merged` by value (`UPDATE person_id`, never delete+reinsert), so post-merge activity survives. Every survivor field the merge changes or the unmerge reverts writes a `person_property_history` row (`source: 'merge'`/`'unmerge'`). `markNotDuplicate` now only transitions `status='open'` → `'not_duplicate'` (conditional update; refuses if the pair is missing or already decided). `src/lib/identity/merge.ts`, `src/lib/identity/mergeDb.ts`, `src/db/schema.ts` (comment only — `person_property_history.source` now documents `'unmerge'`; no migration needed, `merge_event.snapshot` stays jsonb). Tests: `tests/unit/identityMerge.test.ts` — field-kept-after-later-edit, merge-A-then-merge-B-then-unmerge-A keeps B's contribution and a manual edit, moved-connection-with-post-merge-activity, same-BD conflict aggregation reverted vs kept-changed. 247/247 unit tests green, clean `tsc --noEmit`.

## Phase 7: Duplicate Review UI (PR 7, base: PR 6)

- [x] 7.1 **GATE**: owner approved all mockups under `openspec/changes/crm-hubspot-ux/mockups/` (one shared `styles.css`), including `mockups/duplicates.html`, on 2026-09-24.
- [x] 7.2 `/admin/duplicates` page (admin-only, `requireAdmin`) implementing the approved mockup: pair list with pagination, compare panel (recommended survivor + field-level merge preview), Merge / No es duplicado / Deshacer fusión actions, Spanish copy per GLOSSARY.md. Split into three work units: `src/lib/identity/duplicateReviewView.ts` (pure `chooseDefaultSurvivor`/`previewMergeOutcome`, PR 07a) + queries/actions (PR 07a) + `es`/`en` dictionary (PR 07b) + `src/app/admin/duplicates/page.tsx`/CSS (PR 07c), to stay under the 400-line review budget.
- [x] 7.3 E2E: non-admin gets 404 on `/admin/duplicates`; admin merge action produces one `audit_log` row. `tests/e2e/phase7.spec.ts` — written but **not executed** (no real authenticated session, and the non-admin case needs a second account this suite doesn't have yet).
- [x] 7.4 Fresh-review fixes (07b `c868072`, 07c rebased + `4fce7f3`): added the missing `conflicting_strong_keys` reason label (the matcher's other `ReviewReason` value, alongside `name_company`) and a generic fallback so `reasonLabel` never renders a raw key; removed the dead `profile_key`/`verified_email` dictionary entries (those belong to `kind: "auto"` matches, which never reach the review queue's `reasonLabel`). Restored the mockup's unmerge confirmation overlay (`mockups/duplicates.html` ~L105-109) as a server-rendered, no-JS panel: "Deshacer fusión" links to `?confirmUnmerge=<mergeEventId>`, validated against the already-fetched, not-yet-undone history rows (plus a UUID format check) before rendering.

**Intentional deviations from the mockup, left as-is:**
- "Omitir" (skip) button dropped — it was a no-op in the mockup with no distinct behavior from just navigating away.
- History's "Fusionado desde" count column omitted — not tracked anywhere in the schema (`merge_event` has no count of prior source records); would need a new column/derivation to add.
- Survivor is always the recommended one from `chooseDefaultSurvivor` (no manual override in the UI) — matches the approved mockup, which shows the recommendation as the only merge action.
- No confirmation dialog on Merge itself (only on Deshacer/unmerge) — merge is reversible via unmerge, so the extra friction was judged unnecessary; the mockup does not show a merge confirmation either.

## Phase 8: App Shell & Design Tokens (PR 8, base: PR 7; requires Phase 0)

- [x] 8.1 **GATE**: owner approved the design-system mockup (shell, tokens) covering every screen from `/login` onward, including `mockups/design-system.html` and the shell in every screen mockup, on 2026-09-24 (engram #129) — also confirmed the accessibility token tweaks (accent hover darkens instead of lightening; darker badge text).
- [x] 8.2 Built `Sidebar`/`TopBar` app shell under `src/app/contacts/` per design D9 (not yet extracted to `src/components/`): `src/app/contacts/Sidebar.tsx`/`Sidebar.module.css` (moved + reskinned from `src/app/Sidebar.tsx`, same nav items/routes and mobile toggle, sunken-surface/accent-soft visual language from the mockup) and `src/app/contacts/TopBar.tsx`/`TopBar.module.css` (sticky header, contacts-only search per owner decision, submits to `/` using the existing `q` param; breadcrumb derived from the active nav item). Wired into `src/app/layout.tsx`. Intentionally omits the mockup's "Crear"/account dropdowns (duplicate per-page controls like `UserMenu` that later phases migrate page by page) and per-item icons (decorative, not in scope). PR 08c.
- [x] 8.3 Added status/info/badge-text tokens (`--color-success-text`/`--color-warn-text`/`--color-danger-text`/`--color-info`/`--color-info-text`, `--status-*` pairs) and darkened `--color-accent-hover` (`#e6434c` → `#b81f27`, now 6.44:1) in `globals.css`; removed the 14 hardcoded badge hex literals in `companies/[key]/page.tsx` (5 + 2 fallbacks) and `leads/[id]/page.tsx` (5 + 2 fallbacks), replaced with token references. PR 08a.
- [x] 8.4 Removed `LocaleSwitcher` (component + `setLocale` action deleted) and every `localeSwitcher` prop/render call from the UI (login, account/password, discovery, hiring, outreach, whats-new, `UserMenu`); `getLocale()` now always resolves `"es"` (`DEFAULT_LOCALE` changed from `"en"` to `"es"`) instead of reading a cookie. Kept the `{ en, es }` dictionary structure and `isLocale` (still used for the separate outreach message-language feature) per D10. Added the `ClientStrings<T>` guard type (`src/lib/i18n/clientStrings.ts`) and applied it to `GenerateMessageLabels`, `LeadsUploadLabels`, `LeadEditLabels`. PR 08b.
- [x] 8.5 RED/GREEN: `tests/unit/clientStrings.test.ts` — `assertClientStrings`, the runtime counterpart to the `ClientStrings` guard, walks the real `es` dictionary slices (`pickGenerateMessageLabels`, `pickLeadsUploadLabels`, `pickLeadEditLabels`) to guarantee client components only receive plain strings/flat string records, and asserts the full dictionary (which contains formatter functions) is rejected. RED: `assertClientStrings is not a function` before the implementation existed; GREEN: 8/8 passing after adding it. PR 08b.

**Review workload**: split into three chained work units (feature-branch-chain, base `feat/crm-hubspot-ux-07c-duplicates-page`) to stay near the 400-line budget: 08a tokens/badges (61 lines), 08b i18n/ClientStrings (175 lines), 08c app shell (447 raw lines — over budget; mostly a like-for-like restyle of the pre-existing Sidebar file plus two new small shell components landing together as one atomic, low-risk unit. Flagged for the reviewer rather than split further, since Sidebar+TopBar+layout+tokens must land together for the app to compile).

**Intentional deviations from the mockup, left as-is:**
- No per-nav-item icons or live counts (`nav-count`/`pill-count` badges like "16,642") — decorative/data-dependent, not required by tasks.md 8.2.
- TopBar omits the "Crear" quick-create dropdown and the account dropdown — those duplicate existing per-page controls (`UserMenu`, page-specific action buttons) that later phases (9-14) migrate onto this shell one page at a time, not a Phase 8 shell-only concern.
- TopBar search submits to `/` (today's de-facto contacts list), not `/contacts` — that route doesn't exist until Phase 12.
- Kept the pre-Phase-8 CSS layout mechanism (fixed-position sidebar + `margin-left` on `.main-layout`, mobile toggle button) rather than the mockup's CSS-grid `.app`/`.main` wrapper, to preserve the already-working, tested mobile toggle behavior without a layout-system rewrite.

**Fresh-review fixes (PR 08d, branch `feat/crm-hubspot-ux-08d-auth-layout-nav-es`, base 08c, commit `0eeb13c`):**
- **BLOCKER**: `/login` and `/account/password` were rendering inside the app Sidebar/TopBar chrome (root `layout.tsx` wrapped every route). Fixed with Next.js route groups: `src/app/layout.tsx` now holds only html/body/fonts/globals; `src/app/(app)/layout.tsx` renders Sidebar/TopBar for every authenticated page; `src/app/(auth)/layout.tsx` is a bare pass-through for `/login` and `/account/password`, which already render their own centered `.form-narrow` card. `/auth/confirm` (a route handler, no UI) and every `/api/*` route are unaffected by layouts by construction. Moved via `git mv` (diff shows as renames); URLs verified identical before/after via two `next build` route-list captures (25 routes, byte-identical paths). Fixed absolute `@/app/...` imports that pointed at moved subpaths (`companies/AddActivityButton`, `companies/EditCompanyButton`, `companies/actions`, `leads/actions`, `tasks/actions`) and relative imports in the four pages (`discovery`, `hiring`, `outreach`, `whats-new`) that reached the still-root-level `SignOutButton`/`UserMenu`/`BackButton` (now one level deeper, `../../X`).
- **WARNING**: `NAVIGATION` labels (Sidebar) and the TopBar breadcrumb were hardcoded English. Added a `nav` slice to both dictionaries (`es`: Contactos/Empresas/Tareas/Outreach/Vacantes abiertas/Novedades/Descubrimiento/Cuenta, section titles "Espacio de trabajo"/"Señales" per the mockup sidenav groupings; "Outreach" intentionally left untranslated per GLOSSARY.md's "stays as the feature/route name" rule) plus a `ClientStrings`-guarded `pickNavLabels` (`src/lib/i18n/navLabels.ts`, RED/GREEN — extended `tests/unit/clientStrings.test.ts`). `Sidebar`/`TopBar` are client components and now take `labels: NavLabels` as a prop from `(app)/layout.tsx` (a server component) instead of importing the dictionary directly. Breadcrumb fallback for `/` and the sidebar toggle's `aria-label` ("Alternar barra lateral") also moved to the dictionary.
- Verified: `npm run typecheck` clean; `npm run test:unit` 265/265 (264 baseline + 1 new); `npm run build` succeeds, all 25 routes identical to the pre-fix build. Raw diff: 147 changed lines across 57 files (mostly renames; ~143 lines with `-M` rename detection) — well under the 400-line budget, no split needed.

## Phase 9: Record — Shell & About (PR 9, base: PR 8)

- [x] 9.1 **GATE**: owner approved ALL mockups on 2026-09-24, including `mockups/contact-record.html` (three-pane) and `mockups/contact-record-admin.html`.
- [x] 9.2 `/contacts/[id]` route (`src/app/(app)/contacts/[id]/`; `src/app/contacts/` continues to hold only the shared `Sidebar`/`TopBar` shell components — no route clash). Shipped in two PRs: **09b1** (branch `feat/crm-hubspot-ux-09b1-record-shell`, base 09a) — read-only three-pane shell: identity header, properties list with "last updated by X" per property (`src/lib/contacts/queries.ts#getContactRecord`), Empresa/BDs conectados right pane, clickable Actividad/Resumen tabs (`RecordTabs.tsx`) with a Phase-10 placeholder. Merged-person redirect (follows `merged_into_id` chain) and 404 for an unmapped id. **09b2** (branch `feat/crm-hubspot-ux-09b2-about-interactive`, base 09b1) — replaces the read-only property list with the interactive `AboutPane` client component: inline edit per property (wired to 9.4's `updateContactPropertyAction`) and quick actions Nota/Correo/Tarea wired to `createActivityAction`/`createTaskAction`/`sendGmailMessage` (all extended with an optional `personId`); Reunión/Descartar render as disabled placeholders (Phase 10). "Generar mensaje con IA" also render-only placeholder — out of the note/email/task wiring list. Visibility: no `bdId` scoping (contact-record spec).
- [x] 9.3 Spanish copy sourced from the `es` dictionary's new `contactRecord` slice (`src/lib/i18n/dictionaries/{en,es}.ts`), per `GLOSSARY.md`. `src/lib/contacts/labels.ts#pickContactRecordLabels` guards the `AboutPane` client-component boundary (D10/ClientStrings), extended in `tests/unit/clientStrings.test.ts`.
- [x] 9.4 RED/GREEN: property edit writes `person_property_history` row with `changed_by_bd_id`. `src/lib/contacts/propertyEdit.ts` (pure planner, `tests/unit/propertyEdit.test.ts`) + `src/lib/contacts/propertyEditDb.ts` (transactional DB glue) + `src/app/(app)/contacts/actions.ts#updateContactPropertyAction` — PR 9a, branch `feat/crm-hubspot-ux-09a-property-edit-history`. Editable properties: `email`, `jobTitle`, `roleGroup`, `seniority`, `city`, `region`, `country`, `industry`. `ownerBdId` (Responsable) intentionally excluded — reassignment has its own business rule (design R3) a generic property editor would bypass; left read-only for now, documented gap.
- [x] 9.5 (addition, owner-requested alongside Phase 9) `/outreach`'s "view" link now points at `/contacts/[personId]` for every row, including teammate-exclusive Contacts — closes the 5.3 gap noted above. `src/lib/outreach/queries.ts` (`OutreachRow.personId`, optional — `src/lib/whatsnew/queries.ts` still builds `OutreachRow`-shaped rows from the legacy `contact` table and has no unified id yet, out of this task's scope) + `src/app/(app)/outreach/page.tsx` — PR 9c, branch `feat/crm-hubspot-ux-09c-outreach-links`. "Generate message" stays gated on `hasOwnContact`: `generateOutreachMessage` is still `bdId`-scoped on the legacy `contact` row, not trivially safe to enable for a teammate-exclusive Contact within this task's budget.

## Phase 10: Record — Timeline & Log Dialogs (PR 10, base: PR 9)

- [x] 10.1 Middle timeline pane: filterable by activity type; renders `note`, `email_sent`, `hunter_lookup`, `status_change` (read-only), `meeting_logged`, `discarded`, `status_backfill`. PR **10a** (branch `feat/crm-hubspot-ux-10a-timeline`, base 09d). `src/lib/activity/queries.ts#getPersonTimeline` + `src/lib/activity/timelineVisibility.ts` (pure, RED/GREEN) + `src/app/(app)/contacts/[id]/Timeline.tsx`. Filter chips are plain links to `?activityType=`, server re-fetches — no new client JS. `email_sent` from a BD other than the viewer is redacted (locked marker) for every viewer including admins until Phase 11's audited bypass (task 11.3) ships — documented interim R6 default, never over-reveals.
- [x] 10.2 "Log meeting" dialog (Registrar reunión) writing `meeting_logged` activity. PR **10b** (branch `feat/crm-hubspot-ux-10b-meeting-discard`, base 10a). `src/lib/contacts/meeting.ts#planMeeting` (pure, RED/GREEN) + `logContactMeetingAction`. Rendered as the existing inline composer pattern (Nota/Correo/Tarea), not a modal overlay — matches this codebase's established QuickActions convention over the mockup's overlay chrome.
- [x] 10.3 "Discard contact" dialog with fixed reason codes (`wrong_profile`/`not_interested`/`other_vendor`/`left_company`/`bad_data`/`other`); `note` required when reason is `other`. PR **10b**. `src/lib/contacts/discard.ts#planDiscard` + `discardContactAction`. Labels use the owner-confirmed Spanish strings (sdd/crm-hubspot-ux/design-decisions).
- [x] 10.4 RED/GREEN: discard reason validation — `other` without a note is rejected. PR **10b**. `tests/unit/discard.test.ts`.
- [x] 10.5 Board drag-to-status opens the matching log dialog instead of writing status directly (proposal Implications). Implemented alongside 14.1 (PR 14a, branch `feat/crm-hubspot-ux-14a-contacts-board`, base 13c): `src/app/(app)/contacts/BoardDnD.tsx` (drag) and every board card's native "Mover a..." `<details>` menu (no-JS/keyboard fallback) both navigate to `/contacts/[id]?openAction={email|meeting|discard}`, which `src/app/(app)/contacts/[id]/page.tsx`/`QuickActions.tsx` open directly — the write itself still goes through the existing `sendContactEmailAction`/`logContactMeetingAction`/`discardContactAction` from Phase 10. "Nuevo" and "Respondió" have no supported manual log action (no un-log action; replies are detected from inbound messages, not manually logged) — documented no-op for those two columns, not silently mapped to an unrelated action.

## Phase 11: Record — Associations, Admin View, Redirects (PR 11, base: PR 10)

- [x] 11.1 Right associations pane: company, connected BDs (`person_bd_connection`), each with its own `connectedOn`. PR **11a** (branch `feat/crm-hubspot-ux-11a-associations`, base 10b). Company is now a link to `/companies/[key]`. `src/lib/contacts/connectionHistory.ts#describeConnectionHistory` (pure, RED/GREEN) formats each connected BD's message summary (count + last message date) from the `person_bd_connection` aggregates already populated by `recomputeMessageSignals` — no new query against `conversation`/`message`.
- [x] 11.2 Non-admin: shows WHICH BDs have history, never content (R6). PR **11a**. Every viewer (admin or not) sees the same counts/dates-only summary on the associations pane; `describeConnectionHistory`'s return type structurally excludes message content.
- [x] 11.3 `getConversationForAdmin(personId, bdId, actorBdId)`: only path for admin content access; writes `audit_log(view_conversation)` before returning content. PR **11b** (branch `feat/crm-hubspot-ux-11b-admin-conversation`, base 11a). `src/lib/activity/conversationAudit.ts#shouldAuditConversationView` (pure, RED/GREEN: no audit row for a BD viewing their own conversation) + `src/lib/activity/getConversationForAdmin.ts` (one transaction: conditional audit insert, then unredacted `email_sent` activity content for `targetBdId` plus that BD's LinkedIn `conversation`/`message` rows matched on `person.profileKey`). Admin-only UI at `/contacts/[id]/conversation/[bdId]` (`requireAdmin`, 404 for non-admins per design.md "Routes"), linked from the associations pane's "Ver conversación" per connected BD.
- [x] 11.4 `/leads/[id]` and `/contact/[id]` call `redirect()` after a `person_id_map` lookup; own-company rows (`person_id` null) answer 404. PR **11c** (branch `feat/crm-hubspot-ux-11c-legacy-redirects`, base 11b). `src/lib/contacts/legacyRedirectDecision.ts#decideLegacyRedirectFromMap` (pure, DB-free, RED/GREEN) + `src/lib/contacts/legacyRedirect.ts#resolveLegacyRedirectTarget` (person_id_map read + `resolveSurvivor`, now exported from `contacts/queries.ts`). Both legacy detail pages are now redirect-only. **Feature-parity gap found and reported, not silently dropped**: both legacy pages also offered `ManualSignal` ("+ Paste signal", `src/app/ManualSignal.tsx` → `/api/signals/manual`), which the new record page's quick actions (Nota/Correo/Tarea/Reunión/Descartar) do not cover. Everything else (activity timeline, email send, task quick-add) is already covered by the record page.
- [x] 11.5 E2E: admin viewing another BD's conversation produces exactly one audit row; non-admin cannot read content. PR **11c**. `tests/e2e/phase11.spec.ts`, written to the existing `gotoAuthed`/`test.skip` convention (see phase7.spec.ts) — **not executed** in this environment (no DATABASE_URL/live server, and the non-admin case needs a second non-admin session this suite's single-session auth setup doesn't provide).
- [x] 11.6 Closed the 11.4 feature-parity gap: added a "Pegar señal" quick action to `/contacts/[id]` (`QuickActions.tsx`), backed by `src/lib/contacts/manualSignal.ts#planManualSignal` (pure, RED/GREEN) and `src/lib/contacts/manualSignalDb.ts#addManualSignal` (writes `signal.personId` directly — no `person_id_map` lookup needed, unlike the legacy `/api/signals/manual` route). PR **11d** (branch `feat/crm-hubspot-ux-11d-record-manual-signal`, base 11c). Also removed the now-dead `EmailComposer.tsx` (leads/[id]), `ManualSignal.tsx`, and `TaskQuickAdd.tsx` (verified with `rg` — no remaining importers).
- [x] 11.7 Fresh-review fixes on top of PR 11c/11d, same branch: (a) **BLOCKER** — `/contacts/[id]/conversation/[bdId]` validated `id`/`bdId` as UUIDs first (`src/lib/uuid.ts#isUuid`, RED/GREEN) and `getConversationForAdmin` now checks the target `bd` row exists BEFORE writing the `audit_log` insert (its FK previously let a nonexistent/malformed `bdId` throw an uncaught 500 instead of a 404); a merged-away `id` now follows the survivor chain (redirect) instead of 404ing. (b) **WARNING** — `resolveLegacyRedirectTarget` now rejects a non-UUID legacy id (`isValidLegacyId`, RED/GREEN) before it reaches the `person_id_map` query, same failure mode. (c) **SUGGESTION** (list pages linking straight to `/contacts/[personId]`) — deferred: `listLeads`/the home page's contacts query/`whats-new` don't currently select a `personId`, and adding it needs a `person_id_map` join per list query across three separate call sites; out of scope for this batch's size budget, flagged for a follow-up task instead of folded in here.

## Phase 12: List — Views & Filters (PR 12, base: PR 11)

- [x] 12.1 **GATE**: owner approved ALL mockups on 2026-09-24, including `mockups/contacts.html` (views as tabs, filters).
- [x] 12.2 `src/lib/contacts/views.ts`: system views as code constants (All, Mis contactos, Sin contactar, Nuevo con email verificado, Empresas con vacantes, Listo para outreach). PR **12a** (branch `feat/crm-hubspot-ux-12a-views-saved-views`, base 11e). `SYSTEM_VIEWS` + `resolveActiveView` (resolves `?view=` against system views and a BD's saved views).
- [x] 12.3 `saved_view` table CRUD for BD-created views (filters/columns/sort as jsonb). PR **12a**. `src/lib/contacts/savedViewInput.ts#planSavedViewInput` (pure planner, RED/GREEN) + `src/lib/contacts/savedViews.ts` (DB glue: list/get/create/update/delete, every operation scoped to `owner_bd_id` — personal views only, design D7). The `saved_view` table itself already existed in schema (migration 0013); no new migration needed.
- [x] 12.4 RED/GREEN: view filter serialization round-trips through the query string (`?view=`). PR **12a**. `src/lib/contacts/viewFilters.ts#serializeContactFilters`/`parseContactFilters` (pure, `tests/unit/viewFilters.test.ts`) plus `sanitizeContactFilters` for defensively reading a saved view's `filters` jsonb.
- [x] 12.5 (addition, needed to make 12.2-12.4 usable) `/contacts` list page: server-side paginated/filtered table (Nombre/Empresa/Responsable/Estado/Correo), system + saved view tabs, save/delete-view forms (no client JS). PR **12b** (branch `feat/crm-hubspot-ux-12b-contacts-list-query`, base 12a) — `src/lib/contacts/listQueries.ts#getContactListPage`/`getContactCountForFilters` (SQL over `person`, `count(*)::int` cast, reuses existing owner/status/company_key indexes — never loads all ~20.7k rows), `contactList` dictionary slice (es/en), and repoints the TopBar's contacts-only search from `/` to `/contacts`. PR **12c** (branch `feat/crm-hubspot-ux-12c-contacts-list-page`, base 12b) — `src/app/(app)/contacts/page.tsx` + `page.module.css` + `viewActions.ts` (`createSavedViewAction`/`deleteSavedViewAction`). Column picker, bulk-action bar, ad-hoc "Agregar filtro" chips beyond the system views, and the table/board toggle are explicitly out of scope here — Phase 13/14.

## Phase 13: List — Columns & Bulk Actions (PR 13, base: PR 12)

- [x] 13.1 Column picker (Selector de columnas) with persisted column selection per view. PR **13a** (branch `feat/crm-hubspot-ux-13a-column-picker`, base 12c). `src/lib/contacts/columns.ts#sanitizeColumnKeys`/`resolveVisibleColumns` (pure, RED/GREEN, `tests/unit/contactColumns.test.ts`) plus `updateViewColumnsAction` (`viewActions.ts`): persists onto `saved_view.columns` (design D7) when the active view is a saved view; a system view (no DB row) round-trips through a `?columns=` query override instead — documented scope decision, not silently dropped. Extra columns available: `roleGroup`/`industry`/`country`/`source`/`created` (all already on `person`); "BDs conectados"/"Última actividad" from the mockup are deferred (need a join beyond this task's budget).
- [x] 13.2 Bulk actions (Acciones masivas) on multi-row selection. Orchestrator scoped exactly three: Asignar responsable, Crear tarea, Exportar ("Generar mensajes" bulk AI explicitly deferred — see 13.3 for the per-record equivalent). All three done, split across three branches for review budget:
  - PR **13b1a** (branch `feat/crm-hubspot-ux-13b1a-bulk-assign-owner`, base 13a): row selection UI (`BulkActionsBar.tsx`, checkbox column in `page.tsx`) + "Asignar responsable". `src/lib/contacts/bulkOwner.ts#planBulkOwnerAssignment`/`sanitizeBulkPersonIds` (pure, RED/GREEN, `tests/unit/bulkOwner.test.ts`) applies the same R3 rule as `updateLeadOwner` (owner set only when the person has no `person_bd_connection` yet) per selected row, reporting a per-row outcome (assigned/skipped) instead of a silent no-op; `bulkOwnerDb.ts#bulkAssignOwner` writes `person` + `person_property_history` (source `"edit"`, same as `updateLeadOwner`'s dual-write). Selection capped at 200 rows (`MAX_BULK_SELECTION`), truncation surfaced via a `?bulkLimited=1` banner. Budget: raw diff 536+/41- across 11 files (~514 changed lines net of context) — over the 400-line guideline; flagged for sdd-verify as accepted foundational-infra overage (checkbox/selection plumbing is shared by every bulk action) rather than a hard STOP, since `chain_strategy=feature-branch-chain` was already resolved.
  - PR **13b1b** (branch `feat/crm-hubspot-ux-13b1b-bulk-create-task`, base 13b1a): adds "Crear tarea" — one task per selected person sharing the submitted title/due date, via `bulkCreateTaskAction` looping the existing `createTask` (`src/lib/tasks/queries.ts`, same path as the record page's "Tarea" quick action) over the validated id list. 127 changed lines — within budget.
  - PR **13b2** (branch `feat/crm-hubspot-ux-13b2-bulk-export`, base 13b1b): adds "Exportar" — a GET route (`src/app/(app)/contacts/export/route.ts`) streaming a CSV of the selected rows (ids validated/capped via the same `sanitizeBulkPersonIds`) using the active view's visible columns. Pure builder `src/lib/contacts/csvExport.ts#buildContactsCsv` (RED/GREEN, `tests/unit/contactsCsv.test.ts`, 9 cases): RFC4180 escaping (comma/quote/newline), OWASP formula-injection prefixing (`=`/`+`/`-`/`@`/tab/CR), UTF-8 BOM (`CSV_BOM`), Spanish headers from the dictionary. `getContactListRowsByIds` (`listQueries.ts`) fetches the exact selected rows (not the view's filtered/paginated set). ~240 changed code lines (91 more in the test file, excluded) — within budget.
- [x] 13.3 `/leads` and `/outreach` redirect to `/contacts?view=…`; "Generate message" moves to the record page. **`/leads` redirect done in a later batch (PRs 14d1-14d3, see below); `/outreach` explicitly stays out of scope (owner decision pending, per orchestrator instruction).** PR **13c** (branch `feat/crm-hubspot-ux-13c-record-generate-message`, base 13b2): adds "Generar mensaje" to `/contacts/[id]`'s email composer (`QuickActions.tsx#EmailForm`), populating its body field instead of a standalone copy-to-clipboard widget. `src/lib/outreach/personMessageInput.ts#buildPersonMessageInput` (pure, RED/GREEN, `tests/unit/personMessageInput.test.ts`, 7 cases) builds the generator's input from the unified `person` record (name/company/title/role) plus the CALLING BD's own `person_bd_connection` counts/dates and recent `signal.data.body` research notes — works for ANY person, including a teammate-only Contact the calling BD has no connection row for. `history` is always `[]`: this path never reads message content at all, so R6 ("never another BD's conversation content") holds structurally, not by a runtime check. `messagePrompt.ts` gained an optional `notes` field (defaulted, existing `/outreach` caller unaffected) rendered in its own untrusted-data block, same treatment as prior-conversation history. New action `generatePersonOutreachMessageAction` (`contacts/messageActions.ts`) reuses the same `buildOutreachMessagePrompt`/`generateText` call as `/outreach`'s `generateOutreachMessage` — refactored `GenerateMessageButton` to take a pre-bound server action instead of a hardcoded `contactId`, so `/outreach`, `/whats-new`, and the record page all share one component; `historyCount` is fixed at `0` for this path since the existing hint copy describes prior *messages*, which this path never has. `/leads` and `/outreach` legacy-route redirects were **not attempted this batch** — still blocked by the feature-parity guard flagged in the prior apply-progress batch (unmigrated read paths on those pages). Budget: ~430 changed code lines (116 more in the test file, excluded) — ~7.5% over the 400-line guideline; flagged for sdd-verify as an accepted single-deliverable overage (the `GenerateMessageButton` refactor is required infrastructure for reuse, not separable from the feature it enables), consistent with the 13b1a precedent, given `chain_strategy=feature-branch-chain` was already resolved.

**`/leads` feature-parity inventory (this batch, after 14.1/14.2 shipped) — redirect NOT done, parity is incomplete:**

| `/leads` feature | `/contacts` equivalent? | Gap |
|---|---|---|
| CSV upload (Connections/leads with sources) | Yes — task 14.2, `/contacts/import` | None |
| List + pagination | Yes — task 12.5 | None |
| Board (by status) | Yes — task 14.1, but a deliberate redesign (derived status, drag opens a log dialog) not a drop-in replacement of direct-write status | Architectural, not missing |
| Filter: name / company (separate fields) | Partial — one combined free-text `q` search (name+company+email) | No separate per-field filters |
| Filter: `industryGroup` | **No** | `ContactFilters` has no industry filter; `person.industry` is only a displayed/column-picker field |
| Filter: `seniority` | **No** | `person.seniority` exists in the DB but has no filter AND is not in `ALL_CONTACT_COLUMNS` (not even viewable as a column) |
| Filter: owner = a specific BD, or "unassigned" | **No** | `ContactFilters.owner` only supports `"me"` (documented in viewFilters.ts) |
| Filter: `emailStatus` = probable / none (not just verified) | **No** | `ContactFilters.emailVerified` is boolean-only (verified vs. not) |
| Filter: `status` = any of the 5 (ad-hoc, not just system views) | Partial | Only reachable via the 6 fixed system views or a saved view; no ad-hoc status picker in table mode |
| Column: Seniority | **No** | Same DB-vs-column-picker gap as the filter above |
| Per-record owner reassignment (`/leads/[id]`) | **No** | Task 9.4 intentionally excluded `ownerBdId` from the record page's property editor (R3 has its own rule); only the LIST's bulk "Asignar responsable" (task 13.2) can reassign an owner today, not a single record from its own page |
| AI-drafted email (`draftLeadEmailAction`) | Yes — task 13.3, "Generar mensaje" | None |
| Manual signal paste | Yes — task 11.6 | None |

**Conclusion**: redirecting `/leads → /contacts?view=…` is NOT done. Five concrete gaps block it: industryGroup filter, seniority filter+column, owner-by-specific-BD/unassigned filter, granular emailStatus filter, and single-record owner reassignment. `/leads` and `/outreach` stay in place; `/outreach` is explicitly out of scope per this batch's instructions (owner decision pending) and was not inventoried here.

## Phase 14: List — Board & Import (PR 14, base: PR 13)

- [x] 14.1 Table/board toggle by status (`?layout=board`); board covers all Contacts, including LinkedIn-only ones. PR **14a** (branch `feat/crm-hubspot-ux-14a-contacts-board`, base 13c, commit `d38e664`). `src/lib/contacts/board.ts` (pure, RED/GREEN, `tests/unit/contactBoard.test.ts`) + `getContactBoardColumns` (`listQueries.ts`, DB smoke read-only confirmed `count(*)::int` types and null-column shapes) + `Board.tsx`/`BoardDnD.tsx`. **Combined with 10.5 in the same PR** (see 10.5 below) rather than a separate 14b — the drag gesture has nothing to attach to until the board's columns exist, so splitting them would mean shipping a non-functional board first. Budget: 530 raw changed lines (12 files, incl. the 128-line CSS module and a 29-line test file) — over the 400-line guideline; flagged for sdd-verify as an accepted single-deliverable overage, same precedent as 13b1a/13c, given `chain_strategy=feature-branch-chain` is already resolved.
- [x] 14.2 `/contacts/import` moves CSV upload here; shows dedup outcome (auto-merged/flagged/new) using the same matcher as migration. PR **14c** (branch `feat/crm-hubspot-ux-14c-contacts-import`, base 14a, commit `e962c01`). Covers BOTH ingestion sources the app has today: LinkedIn connections CSV (`ConnectionsUploadForm` → the same `uploadCsv`/`upsertContacts` path as the home-page form) and leads CSV with sources (reuses `UploadLeadsForm`/`importLeadsCsv`/`importLeads` as-is). Outcome counts come from the resolver's own `IdentityWritePlan.report` — `runIdentityCutoverChunk` (`src/lib/identity/ingestWrite.ts`) now returns `{ legacyRows, report }` instead of bare `legacyRows`, and a new pure `sumIdentityReports()` (RED/GREEN, `tests/unit/identityIngestWrite.test.ts`) collapses per-chunk reports across a whole import; `upsertContacts`/`importLeads` expose the total as `identityReport` on their result types (additive — both existing callers ignore the extra field). **Deviation, reported not silently dropped**: "moves CSV upload here" implemented as ADDS a consolidated front door — the pre-existing upload controls on `/` (LinkedIn) and `/leads` (leads CSV) are left in place rather than removed/redirected, since fully retiring them touches call sites beyond this PR's budget; `/contacts/import` is a new surface reusing the exact same actions, not a rewrite. No "recent imports" history table (mockup shows one) — no schema exists for persisted import runs; only this-run's outcome is shown, documented gap.
- [x] 14.3 E2E: full flow — import → dedup outcome → contact visible in list and board. `tests/e2e/phase14.spec.ts` (same branch as 14c) — written to the existing `gotoAuthed`/`test.skip` convention (phase7/phase11), **not executed** in this environment (no DATABASE_URL/live server, and it needs a CSV fixture with a guaranteed-new row, supplied via `E2E_IMPORT_CONNECTIONS_CSV_PATH`/`E2E_IMPORT_CONNECTIONS_CSV_NAME`).

## Phase 14d: Close `/leads` Parity Gaps + Redirect (PRs 14d1-14d3, base: PR 14c)

Closes the 5 concrete gaps the prior batch's parity inventory identified, then re-runs the inventory and redirects `/leads` now that parity holds.

- PR **14d1** (branch `feat/crm-hubspot-ux-14d1-list-filters`, base 14c, commit `06b7407`): `industryGroup`/`seniority`/owner(-by-specific-BD/unassigned)/granular-`emailStatus` filters on `/contacts` (table + board), plus a Seniority column. `industryGroup` filters `person.industry` directly (already fed from `lead.industryGroup ?? industryRaw` at ingest — no new column). New indexes `person_industry_idx`/`person_seniority_idx`/`person_email_status_idx` (migration `drizzle/0014_wealthy_micromax.sql`), mirroring the legacy `lead` table's existing indexes. `viewFilters.ts` extended with RED/GREEN round-trip tests (`tests/unit/viewFilters.test.ts`) plus a new `applyAdHocContactFilterOverrides` (ad-hoc filter panel, task 13.1's deferred scope) that layers filter selects on top of the active view's filters, including explicit-clear semantics. Three previously-duplicated column-select blocks in `listQueries.ts` consolidated into one shared `CONTACT_LIST_ROW_COLUMNS`/`baseContactFilterConditions` (DRY refactor, inflates this PR's line count — flagged for sdd-verify as an accepted overage, ~442 raw changed lines, same precedent as 13b1a/13c/14a given `chain_strategy=feature-branch-chain`). DB smoke (read-only): confirmed `count(*)::int` returns numbers, industryGroups has 20 distinct values in prod, seniority currently has 0 (column exists, no data populated yet — filter/column are correct and ready, just unused until data arrives).
- PR **14d2** (branch `feat/crm-hubspot-ux-14d2-record-owner-reassign`, base 14d1, commit `bc66976`): single-record owner reassignment on `/contacts/[id]` — the owner row in `PropertyList` becomes an editable `<select>`, wired to a new `updateContactOwnerAction` that reuses `bulkAssignOwner` (task 13.2) on a one-element selection, so single-record and bulk reassignment apply the exact same R3 rule and can never disagree. Renders locked (no edit affordance) once the person has any connected BD. New pure `normalizeOwnerSelectValue` (`bulkOwner.ts`, RED/GREEN) maps a blank `<select>` to "unassign". ~226 raw changed lines — within budget.
- PR **14d3** (branch `feat/crm-hubspot-ux-14d3-status-filter-and-redirect`, base 14d2, commit `1fb68a8`): closes one more gap found while re-running the inventory — an ad-hoc single-pick status filter on `/contacts` (previously status was only reachable via a fixed system view); then redirects `/leads` to `/contacts?view=...` via a new pure `buildContactsRedirectQuery` (RED/GREEN, `tests/unit/legacyLeadsRedirect.test.ts`) that maps industryGroup/seniority/owner/emailStatus/status/view=board/page straight through (same vocabulary), collapsing `name`+`company` into the combined `q` search. Internal links to `/leads` (sidebar nav, discovery/outreach/hiring/whats-new) now point at `/contacts`; nav label "Leads" → "Contactos" (GLOSSARY.md). Removed the now-dead `/leads/[id]` actions (`updateLeadStatusAction`/`updateLeadOwnerAction`/`draftLeadEmailAction`/`sendLeadEmailAction`) and components (`LeadsBoard.tsx`, `LeadEditForm.tsx`) that lost their only caller once that page became redirect-only (task 11.4) — verified with `rg`, no remaining importers. `/leads/[id]` itself is untouched. Net diff is mostly deletions (997 raw changed lines, ~797 of them deletions of dead code) — flagged for sdd-verify as an accepted single-deliverable overage; splitting the redirect from its required dead-code cleanup would leave orphaned code temporarily.

**Final `/leads` parity table (after 14d1-14d3):**

| `/leads` feature | `/contacts` equivalent? |
|---|---|
| CSV upload (Connections/leads with sources) | Yes — task 14.2, `/contacts/import` |
| List + pagination | Yes — task 12.5 |
| Board (by status) | Yes — task 14.1 (deliberate redesign: derived status, drag opens a log dialog instead of a direct write) |
| Filter: name / company | Yes, as one combined `q` search (superset: also matches email) |
| Filter: `industryGroup` | Yes — PR 14d1 |
| Filter: `seniority` | Yes — PR 14d1 |
| Filter: owner = specific BD / unassigned | Yes — PR 14d1 |
| Filter: `emailStatus` (verified/probable/none) | Yes — PR 14d1 |
| Filter: `status` (any of the 5, ad-hoc) | Yes — PR 14d3 |
| Column: Seniority | Yes — PR 14d1 |
| Per-record owner reassignment | Yes — PR 14d2 |
| AI-drafted email | Yes — task 13.3, "Generar mensaje" |
| Manual signal paste | Yes — task 11.6 |

**Redirect decision**: every tracked feature now has a `/contacts` equivalent, so `/leads` redirects to `/contacts?view=...` (PR 14d3). `/leads/[id]` is unchanged (already redirect-only, task 11.4). `/outreach` is explicitly OUT of this batch's scope (owner decision pending, per orchestrator instruction) and was not touched or re-inventoried.

**Verification (all three PRs, this batch)**: `npx tsc --noEmit` clean at every commit tip. `npm run test:unit` green throughout (376 → 377 → 378 → 385, +9 new tests this batch: viewFilters owner/industryGroup/seniority/emailStatus/ad-hoc-override round-trips, contactColumns seniority, bulkOwner normalizeOwnerSelectValue, legacyLeadsRedirect mapping). `npm run build` clean at every tip, 30 routes preserved (`/leads` shrinks from 16.8 kB to 163 B, matching `/leads/[id]`'s redirect-only size). `git status` clean at every stopping point; `tsconfig.tsbuildinfo` reverted each time. No push, no PR opened (per instruction). `/outreach` was not touched anywhere in this batch.
